# Copy to .dev.vars (git-ignored) for local dev. See wrangler.jsonc for docs.
SESSION_SECRET=replace-with-32-random-bytes-base64
CREDENTIAL_KEY=replace-with-exactly-32-bytes-base64
EMAIL_PROVIDER=console
APP_BASE_URL=http://localhost:8787
